# Story 4.3: Coupon Allocation & Subscription Management

## Status
InProgress

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["turbo typecheck", "vitest", "supabase db push"]

## Story
**As a** subscribed user,
**I want** my coupons allocated automatically and to manage my subscription,
**so that** I can track my benefits and control my plan.

## Acceptance Criteria
1. Apos pagamento confirmado, cupons sao alocados: 10 (mensal) ou 100 (anual) na tabela `coupons`
2. Tabela `coupons`: id, user_id, subscription_id, status (available | used | expired), restaurant_id (null ate uso), hmac_signature, used_at, expires_at, source, created_at
3. Para plano mensal: cupons expiram ao fim do periodo (nao acumulam)
4. Para plano anual: cupons disponiveis durante toda a vigencia da assinatura
5. Tela "Gerenciar Assinatura" no perfil: plano atual, data de renovacao, metodo de pagamento, historico de pagamentos
6. Botao "Cancelar Assinatura" com confirmacao e explicacao do que acontece (acesso ate fim do periodo pago)
7. Ao cancelar, assinatura fica ativa ate o fim do periodo, depois status muda para `expired`

## Tasks / Subtasks
- [ ] Criar/verificar migration da tabela `coupons` (AC: 1, 2)
  - [ ] DDL com enum `coupon_status` e `coupon_source`
  - [ ] HMAC signature default via pgcrypto
  - [ ] RLS policies: user ve proprios, restaurant_admin ve do seu restaurante, super_admin ve todos
  - [ ] Indexes: `user_id, status` / `user_id, restaurant_id, status` / `expires_at WHERE available`
- [ ] Implementar logica de alocacao de cupons (AC: 1, 3, 4)
  - [ ] Verificar que `allocate_coupons` RPC (da Story 4.1) usa `expires_at` correto por plano
  - [ ] Mensal: `now() + INTERVAL '30 days'`
  - [ ] Anual: `now() + INTERVAL '365 days'`
  - [ ] Cupons com `source = 'subscription'`
- [x] Criar Edge Function `expire-coupons` (cron diario) (AC: 3)
  - [x] Query: `UPDATE coupons SET status = 'expired' WHERE status = 'available' AND expires_at < now()`
  - [x] Configurar cron: `0 0 * * *` (meia-noite diariamente)
  - [x] Log de quantidade expirada por execucao
- [x] Criar tela "Gerenciar Assinatura" `apps/mobile/app/(tabs)/profile/subscription.tsx` (AC: 5)
  - [x] Header com nome do plano ativo (Mensal/Anual)
  - [x] Info card: data de inicio, proxima renovacao, status
  - [ ] Metodo de pagamento (ultimo usado, via AbacatePay)
  - [x] Historico de pagamentos (lista com data, valor, status, metodo)
  - [x] Contador de cupons: X disponiveis de Y total
- [ ] Implementar service de historico de pagamentos (AC: 5)
  - [ ] Query `payments` via `subscription_id` ordenado por `created_at DESC`
  - [ ] Formatacao de valores (centavos → R$)
  - [ ] Formatacao de datas (DD/MM/YYYY)
- [x] Implementar fluxo de cancelamento (AC: 6, 7)
  - [x] Botao "Cancelar Assinatura" na tela de gerenciamento
  - [x] Modal/bottom sheet de confirmacao com:
    - Explicacao: "Voce continuara com acesso ate {data_fim_periodo}"
    - Explicacao: "Seus cupons disponiveis continuam validos ate o fim do periodo"
    - Botao "Confirmar cancelamento" (vermelho)
    - Botao "Manter assinatura" (primario)
  - [ ] Chamar API AbacatePay para cancelar assinatura recorrente
  - [ ] Atualizar status local para `cancelled`
  - [ ] Feedback visual: "Assinatura cancelada. Acesso ativo ate {data}."
- [x] Criar Edge Function ou trigger para expirar subscriptions (AC: 7)
  - [x] Cron diario: `UPDATE subscriptions SET status = 'expired' WHERE status = 'cancelled' AND current_period_end < now()`
  - [x] Pode ser combinado com `expire-coupons` cron
- [ ] Testes unitarios e de integracao

## Dev Notes

**Coupons DDL (from Architecture section 5.1):**
```sql
CREATE TYPE coupon_status AS ENUM ('available', 'used', 'expired');
CREATE TYPE coupon_source AS ENUM ('subscription', 'referral', 'review', 'social');

CREATE TABLE coupons (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  subscription_id UUID REFERENCES subscriptions(id),
  status coupon_status DEFAULT 'available',
  restaurant_id UUID REFERENCES restaurants(id),
  hmac_signature TEXT NOT NULL DEFAULT encode(
    hmac(uuid_generate_v4()::text, current_setting('app.hmac_secret', true), 'sha256'),
    'hex'
  ),
  used_at TIMESTAMPTZ,
  expires_at TIMESTAMPTZ NOT NULL,
  source coupon_source DEFAULT 'subscription',
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Indexes
CREATE INDEX idx_coupons_user_status ON coupons(user_id, status);
CREATE INDEX idx_coupons_user_restaurant ON coupons(user_id, restaurant_id, status);
CREATE INDEX idx_coupons_expires ON coupons(expires_at) WHERE status = 'available';
```

**RLS Policies for Coupons (from Architecture section 5.1):**
```sql
ALTER TABLE coupons ENABLE ROW LEVEL SECURITY;

CREATE POLICY "Users view own coupons" ON coupons
  FOR SELECT USING (user_id = auth.uid());

CREATE POLICY "Restaurant admin can view coupons for their restaurant" ON coupons
  FOR SELECT USING (restaurant_id = get_my_restaurant_id());

CREATE POLICY "Super admin view all coupons" ON coupons
  FOR SELECT USING (get_user_role() = 'super_admin');
```

**allocate_coupons RPC (from Architecture section 5.1):**
```sql
CREATE OR REPLACE FUNCTION allocate_coupons(p_user_id UUID, p_subscription_id UUID, p_plan plan_type)
RETURNS INT AS $$
DECLARE
  v_count INT;
  v_expires TIMESTAMPTZ;
BEGIN
  IF p_plan = 'monthly' THEN
    v_count := 10;
    v_expires := now() + INTERVAL '30 days';
  ELSE
    v_count := 100;
    v_expires := now() + INTERVAL '365 days';
  END IF;

  INSERT INTO coupons (user_id, subscription_id, status, expires_at, source)
  SELECT p_user_id, p_subscription_id, 'available', v_expires, 'subscription'
  FROM generate_series(1, v_count);

  RETURN v_count;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

**Subscription Management Screen Specs:**
```
Route: apps/mobile/app/(tabs)/profile/subscription.tsx

Layout:
- Header: "Gerenciar Assinatura"
- Plan badge: "Plano Mensal" ou "Plano Anual" (colored badge)

Info Section:
  - Status: Ativa / Cancelada (com cor)
  - Inicio: DD/MM/YYYY
  - Proxima renovacao: DD/MM/YYYY
  - Cupons: X disponiveis de Y

Payment History:
  - Lista scrollavel
  - Cada item: data | R$ valor | metodo | status (badge)
  - Ordenado por mais recente

Footer:
  - Botao "Cancelar Assinatura" (outline, vermelho)
```

**Cancellation Flow via AbacatePay:**
```typescript
// apps/mobile/services/payments.ts
export async function cancelSubscription(subscriptionId: string) {
  const { data, error } = await supabase.functions.invoke('cancel-subscription', {
    body: { subscription_id: subscriptionId },
  })

  if (error) throw error
  return data
}

// supabase/functions/cancel-subscription/index.ts
// Calls AbacatePay API: POST /subscriptions/:id/cancel
// Then updates local subscriptions table status to 'cancelled'
```

**Regras de negocio importantes:**
- FR5: Mensal R$19,90 = 10 cupons; Anual R$89,90 = 100 cupons
- FR7: Assinaturas sao recorrentes com renovacao automatica
- FR8: Usuario pode cancelar a qualquer momento
- FR9: Cupons NAO acumulam para o proximo mes (mensal)
- Cupons mensais expiram ao fim do periodo de 30 dias
- Cupons anuais expiram ao fim do periodo de 365 dias
- Assinatura cancelada permanece ativa ate `current_period_end`

### Testing
- Unit tests: `allocate_coupons` RPC (10 for monthly, 100 for annual, correct expires_at)
- Unit tests: `expire-coupons` Edge Function (marks expired correctly, skips non-expired)
- Unit tests: cancelation service (calls AbacatePay, updates status)
- Integration tests: full allocation flow — subscription.paid → coupons created
- Integration tests: monthly coupons expire at period end, annual valid for full year
- Integration tests: RLS policies for coupons (user isolation)
- Component tests: subscription management screen shows correct plan info
- Component tests: cancel modal shows correct end date
- Component tests: payment history renders formatted values
- Edge case: user with cancelled subscription can still use coupons until period end
- Edge case: expired subscription → coupons batch-expired by cron
- Edge case: re-subscription after cancellation → new coupons allocated fresh

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 + Architecture v1.0 | Claude (@sm) |
