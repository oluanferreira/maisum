# Story 7.5: Admin Dashboard & Metrics

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["turbo lint", "turbo typecheck", "vitest"]

## Story
**As a** super admin,
**I want** to see platform-wide metrics,
**so that** I can track the business health of +um.

## Acceptance Criteria
1. Dashboard no painel admin +um com metricas globais (FR41)
2. Metricas: total usuarios, assinantes ativos, MRR, cupons resgatados, restaurantes ativos, churn rate (FR41)
3. Grafico de crescimento de assinantes (ultimos 30/90 dias) (FR41)
4. Grafico de cupons resgatados por dia (FR41)
5. Top 5 restaurantes por cupons validados (FR41)
6. Metricas de indicacao: total de referrals, taxa de conversao (FR41)
7. Filtro por cidade (FR41)

## Tasks / Subtasks
- [ ] Implementar RPC `get_admin_metrics` no Supabase (AC: 1, 2, 6)
  - [ ] Retorna JSON com: total_users, active_subscribers, mrr, coupons_redeemed, active_restaurants, total_referrals, referral_conversion
  - [ ] MRR calculado: mensal = R$19,90 (1490 centavos), anual = R$89,90/12 (749 centavos/mes)
  - [ ] Referral conversion: completed / total
- [ ] Implementar RPC `get_admin_metrics_filtered` com filtro por cidade (AC: 7)
  - [ ] Parametro opcional: p_city_id UUID
  - [ ] Se fornecido, filtra usuarios e restaurantes por cidade
- [ ] Implementar churn rate (AC: 2)
  - [ ] Query: subscriptions cancelled no ultimo mes / total ativas no inicio do mes
  - [ ] Adicionar ao retorno de get_admin_metrics
- [ ] Criar pagina de dashboard no admin-web (AC: 1, 2)
  - [ ] Rota: `apps/admin-web/app/(dashboard)/page.tsx`
  - [ ] 6 DashboardCard organisms no topo (2 rows x 3):
    - [ ] Total usuarios (icone users)
    - [ ] Assinantes ativos (icone user-check)
    - [ ] MRR (icone currency-dollar, formato R$ X.XXX)
    - [ ] Cupons resgatados (icone ticket)
    - [ ] Restaurantes ativos (icone storefront)
    - [ ] Churn rate (icone trending-down, formato X.X%)
- [ ] Implementar grafico de crescimento de assinantes (AC: 3)
  - [ ] Recharts LineChart
  - [ ] Query: subscriptions criadas por dia nos ultimos 30/90 dias (acumulativo)
  - [ ] Toggle: 30d | 90d
  - [ ] Cor: secondary (#1B998B)
- [ ] Implementar grafico de cupons por dia (AC: 4)
  - [ ] Recharts BarChart ou LineChart
  - [ ] Query: coupons used por dia
  - [ ] Cor: primary (#FF6B35)
- [ ] Implementar top 5 restaurantes (AC: 5)
  - [ ] Query: JOIN coupons + restaurants, GROUP BY restaurant, COUNT, ORDER DESC LIMIT 5
  - [ ] Tabela simples: posicao, nome, cidade, cupons validados
- [ ] Implementar metricas de referral (AC: 6)
  - [ ] Card ou secao dedicada: total referrals, convertidos, taxa de conversao
  - [ ] Usar dados de get_admin_metrics
- [ ] Implementar filtro por cidade (AC: 7)
  - [ ] Dropdown no topo da pagina com cidades ativas
  - [ ] Ao selecionar, refetch de todas as metricas filtradas
  - [ ] Opcao "Todas as cidades" como default
- [ ] Loading states
  - [ ] Skeleton loading durante fetch
  - [ ] Error state com retry
- [ ] Testes
  - [ ] Unit: DashboardCard, chart components
  - [ ] Integration: get_admin_metrics retorna dados corretos
  - [ ] Integration: filtro por cidade funciona
  - [ ] Integration: churn rate calculado corretamente

## Dev Notes

**get_admin_metrics RPC (from Architecture doc section 5.1):**
```sql
CREATE OR REPLACE FUNCTION get_admin_metrics()
RETURNS JSON AS $$
  SELECT json_build_object(
    'total_users', (SELECT COUNT(*) FROM profiles WHERE role = 'user'),
    'active_subscribers', (SELECT COUNT(*) FROM subscriptions WHERE status = 'active'),
    'mrr', (SELECT COALESCE(SUM(CASE WHEN plan_type = 'monthly' THEN 1490 ELSE 749 END), 0) FROM subscriptions WHERE status = 'active'),
    'coupons_redeemed', (SELECT COUNT(*) FROM coupons WHERE status = 'used'),
    'active_restaurants', (SELECT COUNT(*) FROM restaurants WHERE is_active = true),
    'total_referrals', (SELECT COUNT(*) FROM referrals),
    'referral_conversion', (SELECT COALESCE(
      (COUNT(*) FILTER (WHERE status = 'completed'))::NUMERIC / NULLIF(COUNT(*), 0), 0
    ) FROM referrals)
  );
$$ LANGUAGE sql SECURITY DEFINER STABLE;
```

**Dashboard Layout (from Frontend Spec 4.3.2):**
- 6 metric cards: usuarios totais, assinantes ativos, MRR, cupons resgatados, restaurantes ativos, churn rate
- Grafico: crescimento de assinantes (30/90d)
- Grafico: cupons resgatados por dia
- Top 5 restaurantes por cupons
- Filtro por cidade

**Churn rate query (adicionar ao RPC):**
```sql
-- Churn = cancelled last 30d / active at start of period
'churn_rate', (
  SELECT COALESCE(
    (SELECT COUNT(*) FROM subscriptions WHERE status = 'cancelled' AND updated_at > now() - INTERVAL '30 days')::NUMERIC
    / NULLIF((SELECT COUNT(*) FROM subscriptions WHERE status = 'active'), 0),
    0
  )
)
```

**Subscriber growth chart query:**
```sql
SELECT DATE(created_at) as date, COUNT(*) as new_subscribers,
  SUM(COUNT(*)) OVER (ORDER BY DATE(created_at)) as cumulative
FROM subscriptions
WHERE status IN ('active', 'cancelled')
  AND created_at > now() - INTERVAL '90 days'
GROUP BY DATE(created_at)
ORDER BY date;
```

**Reutilizar DashboardCard de packages/ui** (criado na Story 7.4)

**Recharts:**
- Instalar no admin-web: `npm install recharts`
- LineChart para crescimento, BarChart ou LineChart para cupons/dia
- ResponsiveContainer para responsividade

**File paths:**
- RPC migration: `supabase/migrations/YYYYMMDD_admin_metrics_rpc.sql`
- Dashboard page: `apps/admin-web/app/(dashboard)/page.tsx`
- Subscriber chart: `apps/admin-web/components/subscriber-growth-chart.tsx`
- Coupons chart: `apps/admin-web/components/coupons-daily-chart.tsx`
- Top restaurants: `apps/admin-web/components/top-restaurants.tsx`
- City filter: `apps/admin-web/components/city-filter.tsx`

## Testing
- **Unit:** DashboardCard renders with different value formats (number, currency, percentage)
- **Unit:** City filter dropdown populates correctly
- **Integration:** get_admin_metrics returns correct aggregated data
- **Integration:** Filtered metrics by city return correct subset
- **Integration:** Churn rate calculation matches expected formula
- **Integration:** Subscriber growth chart data is cumulative
- **E2E (manual):** Login as super_admin → see dashboard → toggle chart periods → filter by city → verify numbers match

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 e Architecture v1.0 | @sm |
