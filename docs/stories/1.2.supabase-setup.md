# Story 1.2: Supabase Setup & Base Schema

## Status
Draft

## Executor Assignment
executor: "@data-engineer"
quality_gate: "@dev"
quality_gate_tools: ["supabase start", "supabase db push", "npx supabase gen types typescript --local"]

## Story
**As a** developer,
**I want** Supabase configured with the foundational database schema,
**so that** all apps can connect and operate on shared data.

## Acceptance Criteria
1. Diretorio `supabase/` configurado com `supabase init` contendo `config.toml`, `migrations/`, `functions/`, `seed.sql`
2. Tabelas base criadas via migration: `profiles` (extends auth.users), `restaurants`, `cities`, `coupons`, `subscriptions`
3. Tabela `profiles` inclui: id (FK auth.users ON DELETE CASCADE), full_name, role (user_role ENUM), avatar_url, city_id, referral_code (unique, auto-generated), extra_coupons_this_month, created_at
4. Tabela `restaurants` inclui: id, name, description, address, city_id (FK NOT NULL), phone, cuisine_type, latitude (DOUBLE PRECISION), longitude (DOUBLE PRECISION), photos (TEXT[] DEFAULT '{}'), is_active, admin_user_id (FK profiles), created_at
5. Tabela `cities` inclui: id (UUID), name, state, is_active (DEFAULT true), created_at
6. Todas as ENUM types do sistema criadas: user_role, plan_type, subscription_status, coupon_status, coupon_source, benefit_category, referral_status, social_proof_status, social_proof_type, payment_status, payment_method
7. RLS habilitado em todas as tabelas com policies basicas (users veem seus dados, super_admin ve tudo, cities publicas)
8. Helper functions `get_user_role()` e `get_my_restaurant_id()` criadas como SECURITY DEFINER STABLE
9. Trigger `on_auth_user_created` cria profile automaticamente com role 'user'
10. Seed com cidade "Jequie, BA" ativa
11. `supabase start` roda localmente sem erros
12. `packages/shared` exporta types gerados do Supabase via `supabase gen types typescript --local`

## Tasks / Subtasks
- [ ] Executar `supabase init` no root do monorepo (AC: 1)
  - [ ] Verificar que `supabase/config.toml` foi criado
  - [ ] Configurar `config.toml` com project_id e ports padrao
- [ ] Criar migration 001: ENUM types (AC: 6)
  - [ ] `supabase/migrations/001_enum_types.sql`
  - [ ] Criar todos os 11 ENUMs conforme DDL da arquitetura
- [ ] Criar migration 002: extensions (AC: 1)
  - [ ] `CREATE EXTENSION IF NOT EXISTS "uuid-ossp"`
  - [ ] `CREATE EXTENSION IF NOT EXISTS "pgcrypto"`
- [ ] Criar migration 003: tabelas base (AC: 2, 3, 4, 5)
  - [ ] Tabela `cities` (deve existir antes de profiles e restaurants)
  - [ ] Tabela `profiles` com FK para auth.users e cities
  - [ ] Tabela `restaurants` com FK para cities e profiles
  - [ ] Tabela `restaurant_invites`
  - [ ] Tabela `benefits` e `benefit_rules`
  - [ ] Tabela `subscriptions`
  - [ ] Tabela `coupons` com HMAC default
  - [ ] Tabela `reviews` com UNIQUE(user_id, coupon_id)
  - [ ] Tabela `conversations` com UNIQUE(user_id, restaurant_id)
  - [ ] Tabela `messages`
  - [ ] Tabela `referrals`
  - [ ] Tabela `social_proofs`
  - [ ] Tabela `payments`
  - [ ] Tabela `push_tokens` com UNIQUE(user_id, token)
- [ ] Criar migration 004: indexes (AC: 7)
  - [ ] Indexes para restaurants (city, location, admin)
  - [ ] Indexes para coupons (user_status, user_restaurant, expires)
  - [ ] Indexes para subscriptions, messages, conversations, reviews, referrals, social_proofs, benefits, payments
- [ ] Criar migration 005: RLS policies (AC: 7, 8)
  - [ ] Enable RLS em todas as 15 tabelas
  - [ ] Criar helper function `get_user_role()` (SECURITY DEFINER STABLE)
  - [ ] Criar helper function `get_my_restaurant_id()` (SECURITY DEFINER STABLE)
  - [ ] Policies para profiles (own + super_admin)
  - [ ] Policies para cities (public read + super_admin CRUD)
  - [ ] Policies para restaurants (public active + own + super_admin)
  - [ ] Policies para restaurant_invites (super_admin + public valid)
  - [ ] Policies para subscriptions, coupons, benefits, benefit_rules, reviews, conversations, messages, referrals, social_proofs, payments, push_tokens
- [ ] Criar migration 006: triggers e RPC functions (AC: 9)
  - [ ] Trigger `handle_new_user()` + `on_auth_user_created`
  - [ ] Trigger `update_conversation_timestamp()` + `on_new_message`
  - [ ] RPC `get_restaurant_detail()`
  - [ ] RPC `get_nearby_restaurants()`
  - [ ] RPC `validate_coupon()`
  - [ ] RPC `allocate_coupons()`
  - [ ] RPC `grant_extra_coupons()`
  - [ ] RPC `get_restaurant_metrics()`
  - [ ] RPC `get_admin_metrics()`
- [ ] Criar `supabase/seed.sql` (AC: 10)
  - [ ] Insert cidade Jequie-BA
- [ ] Criar script de geracao de types (AC: 12)
  - [ ] Script em `package.json` root: `"gen:types": "supabase gen types typescript --local > packages/shared/src/types/supabase.ts"`
  - [ ] Exportar types de `packages/shared/src/index.ts`
- [ ] Testar `supabase start` localmente (AC: 11)
  - [ ] Verificar que PostgreSQL, Auth, Realtime e Storage iniciam
  - [ ] Verificar que migrations sao aplicadas automaticamente
  - [ ] Verificar seed data inserido

## Dev Notes

### ENUM Types (completo da arquitetura)

```sql
CREATE TYPE user_role AS ENUM ('user', 'restaurant_admin', 'super_admin');
CREATE TYPE plan_type AS ENUM ('monthly', 'annual');
CREATE TYPE subscription_status AS ENUM ('active', 'cancelled', 'past_due', 'expired');
CREATE TYPE coupon_status AS ENUM ('available', 'used', 'expired');
CREATE TYPE coupon_source AS ENUM ('subscription', 'referral', 'review', 'social');
CREATE TYPE benefit_category AS ENUM ('prato', 'drink', 'sobremesa', 'combo');
CREATE TYPE referral_status AS ENUM ('pending', 'completed');
CREATE TYPE social_proof_status AS ENUM ('pending', 'approved', 'rejected');
CREATE TYPE social_proof_type AS ENUM ('screenshot', 'link');
CREATE TYPE payment_status AS ENUM ('pending', 'paid', 'failed', 'refunded');
CREATE TYPE payment_method AS ENUM ('pix', 'credit_card', 'boleto');
```

### Tabelas DDL (referencia completa)

Todas as tabelas seguem o DDL definido na secao 5.1 do `docs/fullstack-architecture.md`. Pontos-chave:

- **profiles**: `id UUID PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE` — nao tem uuid_generate_v4(), usa o ID do auth.users
- **profiles.referral_code**: `TEXT UNIQUE DEFAULT encode(gen_random_bytes(6), 'hex')` — codigo de indicacao auto-gerado
- **profiles.extra_coupons_this_month**: `INT DEFAULT 0` — contador de cupons extras (reseta mensalmente)
- **restaurants.admin_user_id**: `UUID REFERENCES profiles(id)` — vincula restaurante ao admin
- **coupons.hmac_signature**: Usa `hmac()` com `current_setting('app.hmac_secret', true)`
- **restaurants.photos**: `TEXT[] DEFAULT '{}'` — array PostgreSQL
- **conversations**: `UNIQUE(user_id, restaurant_id)` — 1 conversa por par
- **reviews**: `UNIQUE(user_id, coupon_id)` — 1 review por cupom usado

### Helper Functions (RLS)

```sql
-- Retorna role do usuario autenticado
CREATE OR REPLACE FUNCTION get_user_role()
RETURNS user_role AS $$
  SELECT role FROM profiles WHERE id = auth.uid();
$$ LANGUAGE sql SECURITY DEFINER STABLE;

-- Retorna restaurant_id do admin autenticado
CREATE OR REPLACE FUNCTION get_my_restaurant_id()
RETURNS UUID AS $$
  SELECT id FROM restaurants WHERE admin_user_id = auth.uid() LIMIT 1;
$$ LANGUAGE sql SECURITY DEFINER STABLE;
```

### Trigger handle_new_user

```sql
CREATE OR REPLACE FUNCTION handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO profiles (id, full_name, role)
  VALUES (
    NEW.id,
    COALESCE(NEW.raw_user_meta_data->>'full_name', ''),
    'user'
  );
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION handle_new_user();
```

### Seed Data

```sql
-- supabase/seed.sql
INSERT INTO cities (name, state, is_active) VALUES ('Jequié', 'BA', true);
```

### Script de geracao de types

Adicionar ao `package.json` root:
```json
{
  "scripts": {
    "gen:types": "npx supabase gen types typescript --local > packages/shared/src/types/supabase.ts"
  }
}
```

### Ordem das migrations importa

1. Extensions (uuid-ossp, pgcrypto)
2. ENUMs (usados como column types)
3. Tables (respeitando FK order: cities -> profiles -> restaurants -> ...)
4. Indexes
5. RLS + helper functions
6. Triggers + RPC functions

### Tabelas incluidas nesta story (todas do schema)

| Tabela | FK Dependencies |
|--------|----------------|
| cities | nenhuma |
| profiles | auth.users, cities |
| restaurants | cities, profiles |
| restaurant_invites | restaurants |
| benefits | restaurants |
| benefit_rules | restaurants |
| subscriptions | profiles |
| coupons | profiles, subscriptions, restaurants |
| reviews | profiles, restaurants, coupons |
| conversations | profiles, restaurants |
| messages | conversations, profiles |
| referrals | profiles |
| social_proofs | profiles, restaurants, coupons |
| payments | subscriptions |
| push_tokens | profiles |

## Testing
- `supabase start` deve iniciar sem erros
- `supabase db push` aplica todas as migrations
- `supabase db seed` insere Jequie-BA
- Verificar via Supabase Studio (localhost:54323) que todas as tabelas existem
- Verificar RLS habilitado em todas as tabelas (query `pg_tables` ou Studio)
- Verificar que `gen:types` gera arquivo TypeScript valido
- Testar trigger: criar usuario via Auth API e verificar que profile foi criado com role 'user'
- Testar helper functions via SQL: `SELECT get_user_role()` com usuario autenticado

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 e Architecture v1.0 | River (@sm) |
