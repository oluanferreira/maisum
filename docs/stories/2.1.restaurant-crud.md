# Story 2.1: Restaurant CRUD (Admin +um)

## Status
Approved

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["turbo build", "turbo lint", "turbo typecheck"]

## Story
**As a** super admin,
**I want** to manage restaurants from the admin panel,
**so that** I can onboard and control partner restaurants.

## Acceptance Criteria
1. Pagina de listagem de restaurantes com tabela: nome, cidade, status (ativo/inativo), data de cadastro
2. Formulario de cadastro de restaurante: nome, descricao, endereco, cidade, telefone, coordenadas (lat/lng)
3. Upload de fotos do restaurante (ate 5 fotos, Supabase Storage)
4. Edicao de restaurante existente
5. Toggle ativar/desativar restaurante
6. Busca e filtro por cidade e status
7. Paginacao na listagem

## Tasks / Subtasks
- [ ] Criar pagina de listagem de restaurantes `apps/admin-web/app/(dashboard)/restaurants/page.tsx` (AC: 1)
  - [ ] Implementar componente DataTable com colunas: nome, cidade, status (badge ativo/inativo), data de cadastro
  - [ ] Integrar com Supabase query: `supabase.from('restaurants').select('*, cities(name)')`
  - [ ] Exibir skeleton loading durante carregamento
- [ ] Criar formulario de cadastro `apps/admin-web/app/(dashboard)/restaurants/new/page.tsx` (AC: 2)
  - [ ] Implementar form com React Hook Form + Zod validation
  - [ ] Campos: name, description, address, city_id (dropdown), phone, latitude, longitude
  - [ ] Dropdown de cidades populado via `supabase.from('cities').select('*').eq('is_active', true)`
  - [ ] Submit: `supabase.from('restaurants').insert({...})`
- [ ] Implementar upload de fotos com Supabase Storage (AC: 3)
  - [ ] Criar bucket `restaurant-photos` no Supabase Storage
  - [ ] Componente de upload multi-arquivo (max 5 fotos)
  - [ ] Upload para `restaurant-photos/{restaurant_id}/{filename}`
  - [ ] Salvar URLs publicas no array `photos` da tabela `restaurants`
  - [ ] Preview das fotos com opcao de remover
- [ ] Criar pagina de edicao `apps/admin-web/app/(dashboard)/restaurants/[id]/page.tsx` (AC: 4)
  - [ ] Carregar dados existentes: `supabase.from('restaurants').select('*').eq('id', id).single()`
  - [ ] Reutilizar formulario de cadastro pre-preenchido
  - [ ] Submit: `supabase.from('restaurants').update({...}).eq('id', id)`
- [ ] Implementar toggle ativar/desativar (AC: 5)
  - [ ] Botao na tabela que chama `supabase.from('restaurants').update({ is_active: !current }).eq('id', id)`
  - [ ] Feedback visual (toast de sucesso/erro)
- [ ] Implementar busca e filtros (AC: 6)
  - [ ] Input de busca textual (filtra por nome via `.ilike('name', '%term%')`)
  - [ ] Dropdown filtro por cidade
  - [ ] Dropdown filtro por status (ativo/inativo/todos)
  - [ ] Combinar filtros na query Supabase
- [ ] Implementar paginacao server-side (AC: 7)
  - [ ] Usar `.range(from, to)` do Supabase para paginacao
  - [ ] Componente de paginacao com pagina atual, total de paginas, botoes prev/next
  - [ ] Default: 20 itens por pagina

## Dev Notes

**Tabela `restaurants` (DDL do Architecture doc):**
```sql
CREATE TABLE restaurants (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  name TEXT NOT NULL,
  description TEXT,
  address TEXT NOT NULL,
  city_id UUID NOT NULL REFERENCES cities(id),
  phone TEXT,
  cuisine_type TEXT,
  latitude DOUBLE PRECISION NOT NULL,
  longitude DOUBLE PRECISION NOT NULL,
  photos TEXT[] DEFAULT '{}',
  is_active BOOLEAN DEFAULT true,
  admin_user_id UUID REFERENCES profiles(id),
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Indexes relevantes
CREATE INDEX idx_restaurants_city ON restaurants(city_id) WHERE is_active = true;
CREATE INDEX idx_restaurants_admin ON restaurants(admin_user_id);
```

**RLS Policies (super admin tem acesso total):**
```sql
CREATE POLICY "Super admin full access" ON restaurants FOR ALL USING (get_user_role() = 'super_admin');
CREATE POLICY "Anyone can view active restaurants" ON restaurants FOR SELECT USING (is_active = true);
```

**Supabase Storage — Bucket config:**
```sql
-- Criar bucket via Supabase Dashboard ou migration
INSERT INTO storage.buckets (id, name, public) VALUES ('restaurant-photos', 'restaurant-photos', true);

-- Policy: super_admin pode fazer upload
CREATE POLICY "Super admin upload" ON storage.objects FOR INSERT
  WITH CHECK (bucket_id = 'restaurant-photos' AND (SELECT role FROM profiles WHERE id = auth.uid()) = 'super_admin');

-- Policy: qualquer um pode ler (fotos publicas)
CREATE POLICY "Public read" ON storage.objects FOR SELECT USING (bucket_id = 'restaurant-photos');
```

**Rotas do admin-web (estrutura de arquivos):**
```
apps/admin-web/app/(dashboard)/restaurants/
├── page.tsx              # Listagem com tabela, filtros, paginacao
├── new/page.tsx          # Formulario de criacao
└── [id]/page.tsx         # Formulario de edicao
```

**Supabase Client (server-side para SSR):**
```typescript
// apps/admin-web/lib/supabase/server.ts
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { Database } from '@maisum/shared/types/supabase'

export const createClient = () => {
  const cookieStore = cookies()
  return createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { getAll: () => cookieStore.getAll() } }
  )
}
```

**TypeScript Interface (packages/shared):**
```typescript
export interface Restaurant {
  id: string;
  name: string;
  description: string;
  address: string;
  city_id: string;
  phone: string;
  latitude: number;
  longitude: number;
  photos: string[];
  cuisine_type: string;
  is_active: boolean;
  created_at: string;
}
```

**Zod Schema para validacao do form:**
```typescript
import { z } from 'zod'

export const restaurantSchema = z.object({
  name: z.string().min(2, 'Nome obrigatorio'),
  description: z.string().optional(),
  address: z.string().min(5, 'Endereco obrigatorio'),
  city_id: z.string().uuid('Selecione uma cidade'),
  phone: z.string().optional(),
  cuisine_type: z.string().optional(),
  latitude: z.number().min(-90).max(90),
  longitude: z.number().min(-180).max(180),
})
```

**Componentes de UI (packages/ui):** Usar shadcn/ui components — DataTable, Button, Input, Select, Badge, Dialog, Toast.

### Testing
- Unit: Zod schema validation tests (restaurantSchema)
- Integration: Supabase query tests (CRUD operations com RLS)
- UI: Verificar que tabela renderiza com dados mockados
- Manual: Testar upload de fotos, filtros, paginacao no browser

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 + Architecture v1.0 | Claude (@sm) |
