# Story 5.2: QR Code Validation (Restaurant Side)

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["vitest", "turbo lint", "turbo typecheck"]

## Story
**As a** restaurant admin,
**I want** to validate customer coupons via QR scanner,
**so that** I can confirm the "+um" benefit.

## Acceptance Criteria
1. Pagina "Validar Cupom" no painel do restaurante com camera ativa para scanner QR (FR19, FR51)
2. Alternativa: campo de input para digitar codigo manualmente (FR19)
3. Ao escanear/digitar, sistema verifica: assinatura HMAC valida, cupom nao expirado, cupom nao usado, restaurante correto (FR17, FR18)
4. Se valido: tela verde com "Cupom Validado!" + nome do cliente + item do beneficio (FR20)
5. Se invalido: tela vermelha com motivo especifico (expirado, ja usado, restaurante errado, assinatura invalida)
6. Apos validacao, cupom marcado como `used` com timestamp e restaurant_id (FR20)
7. Historico de cupons validados hoje na mesma pagina
8. Funciona em tablet e smartphone (camera frontal ou traseira)

## Tasks / Subtasks
- [ ] Criar pagina `apps/restaurant-web/app/(dashboard)/validate/page.tsx` (AC: 1)
  - [ ] Layout com camera view ocupando ~60% da tela superior
  - [ ] Titulo "Validar Cupom" no header
  - [ ] Responsive: funciona em tablet e smartphone
- [ ] Implementar scanner QR via camera do browser (AC: 1, 8)
  - [ ] Usar `html5-qrcode` ou `@yudiel/react-qr-scanner` para web
  - [ ] Suportar camera frontal e traseira (toggle)
  - [ ] Overlay com guia visual de enquadramento
  - [ ] Auto-detect QR Code no frame da camera
  - [ ] Solicitar permissao de camera com fallback para input manual
- [ ] Implementar input manual como alternativa (AC: 2)
  - [ ] Campo de texto "Ou digite o codigo:" + botao "Validar"
  - [ ] Aceitar coupon_id (UUID) digitado manualmente
  - [ ] Validacao de formato UUID antes de enviar
- [ ] Implementar funcao `validateQRPayload` no client (AC: 3)
  - [ ] Parsear JSON do QR Code para extrair `QRPayload`
  - [ ] Verificar expiracao local (< 15 minutos)
  - [ ] Verificar HMAC-SHA256 localmente como pre-validacao
  - [ ] Chamar RPC `validate_coupon` no Supabase para validacao final server-side
- [ ] Implementar overlay de sucesso (AC: 4)
  - [ ] Background: `--color-success` (#22C55E) com opacity
  - [ ] Icone: check grande animado (CSS animation ou Lottie)
  - [ ] Texto: "Cupom Validado!" + nome do cliente + beneficio resgatado
  - [ ] Auto-dismiss apos 5 segundos ou tap para fechar
- [ ] Implementar overlay de erro (AC: 5)
  - [ ] Background: `--color-error` (#EF4444) com opacity
  - [ ] Icone: X animado
  - [ ] Texto: motivo especifico (mapeamento de error codes)
  - [ ] Motivos: "QR Code expirado", "Cupom ja utilizado", "Restaurante incorreto", "Assinatura invalida", "Cupom nao encontrado"
  - [ ] Auto-dismiss apos 5 segundos ou tap para fechar
- [ ] Integrar com RPC `validate_coupon` do Supabase (AC: 3, 6)
  - [ ] Chamar `supabase.rpc('validate_coupon', { p_coupon_id, p_restaurant_id })`
  - [ ] Tratar resposta: `{ valid: true, user_name, coupon_id }` ou `{ valid: false, reason }`
  - [ ] Cupom marcado como `used` pelo proprio RPC (server-side)
- [ ] Implementar historico de validacoes do dia (AC: 7)
  - [ ] Query: `coupons` WHERE `restaurant_id = myRestaurantId` AND `used_at::date = today` ORDER BY `used_at DESC`
  - [ ] Join com `profiles` para nome do cliente e `benefits` para item resgatado
  - [ ] Listagem abaixo do scanner: icone check + nome + hora + item
  - [ ] Contador "Validados Hoje (N)"
  - [ ] Auto-refresh apos cada validacao
- [ ] Garantir prioridade tablet e touch-friendly (AC: 8)
  - [ ] Botoes minimo 48px de altura (touch target)
  - [ ] Layout responsivo: sidebar vira drawer em < 1024px
  - [ ] Camera view se ajusta ao viewport
  - [ ] Input manual com teclado virtual em tablets

## Dev Notes

**validate_coupon RPC (from Architecture doc section 5.1):**
```sql
-- Server-side: marca cupom como usado atomicamente
CREATE OR REPLACE FUNCTION validate_coupon(p_coupon_id UUID, p_restaurant_id UUID)
RETURNS JSON AS $$
DECLARE
  v_coupon coupons;
BEGIN
  SELECT * INTO v_coupon FROM coupons WHERE id = p_coupon_id;

  IF v_coupon IS NULL THEN
    RETURN json_build_object('valid', false, 'reason', 'Cupom nao encontrado');
  END IF;

  IF v_coupon.status != 'available' THEN
    RETURN json_build_object('valid', false, 'reason', 'Cupom ja utilizado ou expirado');
  END IF;

  IF v_coupon.expires_at < now() THEN
    UPDATE coupons SET status = 'expired' WHERE id = p_coupon_id;
    RETURN json_build_object('valid', false, 'reason', 'Cupom expirado');
  END IF;

  UPDATE coupons
  SET status = 'used', used_at = now(), restaurant_id = p_restaurant_id
  WHERE id = p_coupon_id;

  RETURN json_build_object(
    'valid', true,
    'user_name', (SELECT full_name FROM profiles WHERE id = v_coupon.user_id),
    'coupon_id', p_coupon_id
  );
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

**validateQRPayload client-side (from Architecture doc section 6.3):**
```typescript
// apps/restaurant-web/utils/qr-validation.ts
interface QRPayload {
  coupon_id: string
  user_id: string
  restaurant_id: string
  timestamp: number
  signature: string
}

function validateQRPayload(
  payload: QRPayload,
  secret: string
): { valid: boolean; reason?: string } {
  // Check expiration (15 minutes)
  const age = Date.now() - payload.timestamp
  if (age > 15 * 60 * 1000) {
    return { valid: false, reason: 'QR Code expirado' }
  }

  // Verify HMAC
  const data = `${payload.coupon_id}:${payload.user_id}:${payload.restaurant_id}:${payload.timestamp}`
  const expectedSig = hmacSHA256(data, secret)
  if (payload.signature !== expectedSig) {
    return { valid: false, reason: 'Assinatura invalida' }
  }

  return { valid: true }
}
```

**QR Validation screen specs (from Frontend Spec section 4.2.3):**
```
┌──────────────────────────────────┐
│  Validar Cupom                   │
│                                   │
│  ┌──────────────────────────┐    │
│  │                          │    │
│  │     CAMERA VIEW          │    │
│  │     Aponte para o QR     │    │
│  │                          │    │
│  └──────────────────────────┘    │
│                                   │
│  Ou digite o codigo:             │
│  [____________] [Validar]        │
│                                   │
│  ─── Validados Hoje (3) ───     │
│  ✅ Joao · 14:32 · Pizza         │
│  ✅ Maria · 13:15 · Drink        │
│  ✅ Carlos · 12:01 · Sobremesa   │
└──────────────────────────────────┘
```

**Success/Error overlay specs:**
- Sucesso: bg `--color-success` (#22C55E) com opacity, check animado (Lottie), "Cupom Validado!" + nome + beneficio
- Erro: bg `--color-error` (#EF4444) com opacity, X animado, motivo especifico

**Camera API setup (web):**
```typescript
// Using html5-qrcode for browser camera access
import { Html5QrcodeScanner } from 'html5-qrcode'

const scanner = new Html5QrcodeScanner('reader', {
  fps: 10,
  qrbox: { width: 250, height: 250 },
  rememberLastUsedCamera: true,
})

scanner.render(
  (decodedText) => {
    const payload = JSON.parse(decodedText) as QRPayload
    handleValidation(payload)
  },
  (error) => {
    // QR not detected in frame — silent, keep scanning
  }
)
```

**Error code mapping:**
```typescript
const ERROR_MESSAGES: Record<string, string> = {
  'QR Code expirado': 'O QR Code expirou. Peca ao cliente para regenerar.',
  'Cupom ja utilizado ou expirado': 'Este cupom ja foi usado ou expirou.',
  'Cupom nao encontrado': 'Cupom nao encontrado no sistema.',
  'Assinatura invalida': 'QR Code invalido. Possivel fraude.',
}
```

**File structure:**
```
apps/restaurant-web/
├── app/(dashboard)/validate/page.tsx   # Validation page
├── components/
│   ├── qr-scanner.tsx                  # Camera QR scanner component
│   ├── manual-code-input.tsx           # Manual input fallback
│   ├── validation-overlay.tsx          # Success/error overlay
│   └── today-validations.tsx           # Today's history list
├── utils/qr-validation.ts             # validateQRPayload function
└── hooks/useQRScanner.ts              # Camera permission + scanning hook
```

## Testing
- **Unit:** `validateQRPayload` rejeita payload expirado (> 15min)
- **Unit:** `validateQRPayload` rejeita HMAC invalido
- **Unit:** `validateQRPayload` aceita payload valido
- **Unit:** Error code mapping retorna mensagem correta para cada motivo
- **Integration:** RPC `validate_coupon` marca cupom como `used` no banco
- **Integration:** RPC `validate_coupon` rejeita cupom ja usado
- **Integration:** Historico de validacoes mostra apenas cupons do dia atual
- **Integration:** Scanner QR parseia payload JSON corretamente
- **Lint:** `turbo lint` passa sem erros
- **Typecheck:** `turbo typecheck` passa sem erros

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 e Architecture v1.0 | Claude (@sm) |
