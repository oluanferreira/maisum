# Story 7.1: Review System

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["turbo lint", "turbo typecheck", "vitest"]

## Story
**As a** client user,
**I want** to rate my experience after using a coupon,
**so that** I help other users and earn an extra coupon.

## Acceptance Criteria
1. Tabela `reviews` criada: id, user_id, restaurant_id, coupon_id, rating (1-5), comment, created_at (FR30)
2. Apos validacao de cupom, app mostra prompt "Avaliar experiencia" (pode ser adiado) (FR30)
3. Tela de avaliacao: 5 estrelas selecionaveis + campo de comentario opcional (FR30)
4. Ao submeter, usuario ganha 1 cupom extra (respeitando limite de 10 extras/mes) (FR32)
5. Feedback visual: "Voce ganhou +1 cupom!" (FR32)
6. Avaliacoes aparecem na pagina do restaurante (nota media + lista) (FR31)
7. Restaurante ve avaliacoes no painel com nota, comentario e data (FR53)
8. RLS: usuario pode criar avaliacao, nao pode editar/deletar. Restaurante pode ler, nao pode deletar

## Tasks / Subtasks
- [ ] Criar migration para tabela `reviews` (AC: 1)
  - [ ] DDL com campos: id UUID PK, user_id FK, restaurant_id FK, coupon_id FK, rating INT CHECK(1-5), comment TEXT, created_at TIMESTAMPTZ
  - [ ] Constraint UNIQUE(user_id, coupon_id) — 1 review por uso de cupom
  - [ ] Indexes: idx_reviews_restaurant (restaurant_id, rating), idx_reviews_user (user_id)
- [ ] Configurar RLS policies para `reviews` (AC: 8)
  - [ ] "Anyone can view reviews" — SELECT com USING (true)
  - [ ] "Users can create reviews" — INSERT com WITH CHECK (user_id = auth.uid())
  - [ ] "Super admin can manage reviews" — ALL com USING (get_user_role() = 'super_admin')
- [ ] Implementar RPC `grant_extra_coupons` (AC: 4)
  - [ ] Verifica extra_coupons_this_month + count <= 10
  - [ ] Insere cupons com source 'review' e expires_at do periodo da assinatura
  - [ ] Incrementa extra_coupons_this_month no profile
  - [ ] Retorna false se limite atingido
- [ ] Criar tela de avaliacao no mobile — modal bottom sheet (AC: 2, 3)
  - [ ] Componente StarRating atom: 5 estrelas 40px, tap area 48px, cor accent (#FFCB47)
  - [ ] Campo de comentario opcional (TextInput multiline)
  - [ ] CTA "Enviar e ganhar +1 cupom" com destaque no cupom extra
  - [ ] Botao "Avaliar depois" para adiar
  - [ ] Trigger apos validacao de cupom (coupon status → used)
- [ ] Feedback visual "+1 cupom!" (AC: 5)
  - [ ] Animacao de sucesso apos submit (scale + fade)
  - [ ] Toast/banner com mensagem de cupom extra ganho
  - [ ] Tratamento de erro se limite de extras atingido
- [ ] Exibir avaliacoes na pagina do restaurante (AC: 6)
  - [ ] Secao de avaliacoes no RestaurantDetail: nota media + contagem + lista das ultimas 5
  - [ ] Componente ReviewCard molecule: avatar, nome, estrelas, comentario, data
  - [ ] Paginacao (load more) para ver mais avaliacoes
- [ ] Painel restaurante — pagina de avaliacoes (AC: 7)
  - [ ] Rota `apps/restaurant-web/app/(dashboard)/reviews/page.tsx`
  - [ ] Tabela com colunas: cliente, nota, comentario, data
  - [ ] Filtro por periodo (7d, 30d, 90d)
  - [ ] Nota media no topo da pagina
- [ ] Atualizar RPC `get_restaurant_detail` para incluir avg_rating e review_count (AC: 6)
- [ ] Testes
  - [ ] Unit: validacao de rating (1-5), grant_extra_coupons logic
  - [ ] Integration: RLS — user cria review, nao edita/deleta; restaurant le, nao deleta
  - [ ] Integration: UNIQUE constraint (user_id, coupon_id)

## Dev Notes

**Reviews Table DDL (from Architecture doc section 5.1):**
```sql
CREATE TABLE reviews (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  user_id UUID NOT NULL REFERENCES profiles(id) ON DELETE CASCADE,
  restaurant_id UUID NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  coupon_id UUID REFERENCES coupons(id),
  rating INT NOT NULL CHECK (rating >= 1 AND rating <= 5),
  comment TEXT,
  created_at TIMESTAMPTZ DEFAULT now(),
  UNIQUE(user_id, coupon_id) -- 1 review per coupon use
);

CREATE INDEX idx_reviews_restaurant ON reviews(restaurant_id, rating);
CREATE INDEX idx_reviews_user ON reviews(user_id);
```

**RLS Policies (from Architecture doc):**
```sql
CREATE POLICY "Anyone can view reviews" ON reviews FOR SELECT USING (true);
CREATE POLICY "Users can create reviews" ON reviews FOR INSERT WITH CHECK (user_id = auth.uid());
CREATE POLICY "Super admin can manage reviews" ON reviews FOR ALL USING (get_user_role() = 'super_admin');
```

**grant_extra_coupons RPC (from Architecture doc section 5.1):**
```sql
CREATE OR REPLACE FUNCTION grant_extra_coupons(p_user_id UUID, p_count INT, p_source coupon_source)
RETURNS BOOLEAN AS $$
DECLARE
  v_current INT;
  v_sub subscriptions;
BEGIN
  SELECT extra_coupons_this_month INTO v_current FROM profiles WHERE id = p_user_id;
  IF v_current + p_count > 10 THEN
    RETURN false; -- limite de 10 extras/mes
  END IF;

  SELECT * INTO v_sub FROM subscriptions WHERE user_id = p_user_id AND status = 'active' LIMIT 1;
  IF v_sub IS NULL THEN
    RETURN false;
  END IF;

  INSERT INTO coupons (user_id, subscription_id, status, expires_at, source)
  SELECT p_user_id, v_sub.id, 'available', v_sub.current_period_end, p_source
  FROM generate_series(1, p_count);

  UPDATE profiles SET extra_coupons_this_month = extra_coupons_this_month + p_count WHERE id = p_user_id;
  RETURN true;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

**Review Screen (from Frontend Spec 4.1.9):**
- Modal bottom sheet apos uso do cupom
- Stars: 40px, tap area 48px, animacao de pulse ao selecionar
- Cor estrelas: `--color-accent` (#FFCB47) filled, `--color-neutral-300` empty
- CTA destaca cupom extra como incentivo
- Pode ser adiado (mostra novamente ao abrir app)

**Star Rating Atom (from Frontend Spec 2.8):**
- 5 estrelas, cor `--color-accent` (filled) / `--color-neutral-300` (empty)
- Size: 20px (inline/display), 32px (input de avaliacao na tela de review usa 40px)
- Suporta meia estrela na exibicao
- Touch target 44px no modo input

**File paths:**
- Migration: `supabase/migrations/YYYYMMDD_create_reviews.sql`
- Mobile review screen: `apps/mobile/app/review/[restaurantId].tsx`
- StarRating atom: `apps/mobile/components/atoms/StarRating.tsx`
- ReviewCard molecule: `apps/mobile/components/molecules/ReviewCard.tsx`
- Restaurant reviews panel: `apps/restaurant-web/app/(dashboard)/reviews/page.tsx`
- Service: `apps/mobile/services/reviews.ts`

## Testing
- **Unit:** Zod schema validation for review input (rating 1-5, comment optional)
- **Unit:** grant_extra_coupons returns false when limit reached
- **Integration:** RLS policies — user creates, cannot edit/delete; restaurant reads only
- **Integration:** UNIQUE(user_id, coupon_id) prevents duplicate reviews per coupon
- **Integration:** Extra coupon count increments correctly in profile
- **E2E (manual):** Full flow — use coupon → rate → receive extra coupon → see on restaurant page

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 e Architecture v1.0 | @sm |
