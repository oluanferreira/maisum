# Story 7.4: Restaurant Dashboard & Metrics

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["turbo lint", "turbo typecheck", "vitest"]

## Story
**As a** restaurant admin,
**I want** to see metrics about my performance on the platform,
**so that** I understand the value +um brings to my business.

## Acceptance Criteria
1. Dashboard no painel do restaurante com metricas do periodo selecionavel (7d, 30d, 90d) (FR49)
2. Metricas: cupons validados, clientes unicos, nota media, total de avaliacoes (FR49)
3. Grafico de cupons validados por dia (ultimo periodo) (FR49)
4. Lista de beneficios mais resgatados (ranking) (FR49)
5. Dados carregados via queries otimizadas (RPC no Supabase)

## Tasks / Subtasks
- [ ] Implementar RPC `get_restaurant_metrics` no Supabase (AC: 5)
  - [ ] Parametros: p_restaurant_id UUID, p_days INT DEFAULT 30
  - [ ] Retorna JSON com: coupons_validated, unique_customers, avg_rating, total_reviews, daily_coupons (array date/count)
  - [ ] Queries otimizadas usando indexes existentes
- [ ] Criar pagina de dashboard no painel restaurante (AC: 1, 2)
  - [ ] Rota: `apps/restaurant-web/app/(dashboard)/page.tsx`
  - [ ] Seletor de periodo: botoes 7d | 30d | 90d (estado local)
  - [ ] 4 DashboardCard organisms no topo:
    - [ ] Cupons validados (icone ticket, cor primary)
    - [ ] Clientes unicos (icone users, cor secondary)
    - [ ] Nota media (icone star, cor accent)
    - [ ] Total de avaliacoes (icone chat, cor info)
- [ ] Implementar grafico de cupons por dia (AC: 3)
  - [ ] Recharts LineChart responsivo
  - [ ] Eixo X: datas, Eixo Y: quantidade de cupons
  - [ ] Tooltip com data e contagem
  - [ ] Cores: linha primary (#FF6B35), area com gradiente
- [ ] Implementar ranking de beneficios resgatados (AC: 4)
  - [ ] Query: JOIN coupons + benefits agrupado por benefit_name, COUNT, ORDER DESC
  - [ ] Lista com posicao, nome do beneficio, categoria, quantidade
  - [ ] Limite: top 10
- [ ] Componente DashboardCard organism (AC: 2)
  - [ ] Props: title, value, icon, color, trend (opcional)
  - [ ] Layout: icone a esquerda, valor grande + label a direita
  - [ ] Background suave da cor do card
  - [ ] Reutilizavel em `packages/ui` para admin-web tambem usar
- [ ] Loading states e empty states (AC: 1)
  - [ ] Skeleton loading durante fetch
  - [ ] Empty state quando nao ha dados no periodo
- [ ] Testes
  - [ ] Unit: DashboardCard rendering com diferentes props
  - [ ] Integration: get_restaurant_metrics retorna dados corretos
  - [ ] Integration: RPC responde em < 500ms com dados de teste

## Dev Notes

**get_restaurant_metrics RPC (from Architecture doc section 5.1):**
```sql
CREATE OR REPLACE FUNCTION get_restaurant_metrics(p_restaurant_id UUID, p_days INT DEFAULT 30)
RETURNS JSON AS $$
  SELECT json_build_object(
    'coupons_validated', (SELECT COUNT(*) FROM coupons WHERE restaurant_id = p_restaurant_id AND used_at > now() - (p_days || ' days')::INTERVAL),
    'unique_customers', (SELECT COUNT(DISTINCT user_id) FROM coupons WHERE restaurant_id = p_restaurant_id AND used_at > now() - (p_days || ' days')::INTERVAL),
    'avg_rating', (SELECT COALESCE(AVG(rating)::NUMERIC(2,1), 0) FROM reviews WHERE restaurant_id = p_restaurant_id),
    'total_reviews', (SELECT COUNT(*) FROM reviews WHERE restaurant_id = p_restaurant_id),
    'daily_coupons', (
      SELECT json_agg(json_build_object('date', d, 'count', c))
      FROM (
        SELECT used_at::DATE as d, COUNT(*) as c
        FROM coupons
        WHERE restaurant_id = p_restaurant_id AND used_at > now() - (p_days || ' days')::INTERVAL
        GROUP BY used_at::DATE ORDER BY d
      ) sub
    )
  );
$$ LANGUAGE sql SECURITY DEFINER STABLE;
```

**Dashboard Layout (from Frontend Spec 4.2.2):**
- 4 metric cards no topo: cupons validados, clientes unicos, nota media, total avaliacoes
- Grafico de linha: cupons por dia (ultimos 30d)
- Lista: top beneficios resgatados
- Periodo selecionavel: 7d | 30d | 90d

**DashboardCard Organism specs:**
- Componente em `packages/ui/src/dashboard-card.tsx` (compartilhado com admin-web)
- Background: cor do card com opacity 0.1
- Icone: Phosphor Icons, 24px, cor do card
- Value: font-display (32px, 700)
- Label: font-caption (12px, neutral-600)
- Border radius: `--radius-md` (10px)
- Shadow: `--shadow-sm`

**Recharts config:**
- Instalar: `npm install recharts` no workspace restaurant-web
- LineChart com ResponsiveContainer
- Cores alinhadas com design system

**File paths:**
- RPC migration: `supabase/migrations/YYYYMMDD_restaurant_metrics_rpc.sql`
- Dashboard page: `apps/restaurant-web/app/(dashboard)/page.tsx`
- DashboardCard: `packages/ui/src/dashboard-card.tsx`
- Chart component: `apps/restaurant-web/components/coupons-chart.tsx`
- Benefits ranking: `apps/restaurant-web/components/benefits-ranking.tsx`

## Testing
- **Unit:** DashboardCard component renders title, value, icon correctly
- **Unit:** Period selector toggles correctly between 7d/30d/90d
- **Integration:** get_restaurant_metrics RPC returns correct aggregated data
- **Integration:** RPC performance — responds in < 500ms with test dataset
- **Integration:** Daily coupons chart data matches actual coupon usage
- **E2E (manual):** Login as restaurant admin → see dashboard with real data → toggle periods → verify numbers

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 e Architecture v1.0 | @sm |
