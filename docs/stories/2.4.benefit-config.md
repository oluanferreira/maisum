# Story 2.4: Benefit Configuration

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["turbo build", "turbo lint", "turbo typecheck"]

## Story
**As a** restaurant admin,
**I want** to configure which items are part of the "+um" benefit,
**so that** I control exactly what clients receive.

## Acceptance Criteria
1. Tabela `benefits` criada: id, restaurant_id, name, description, category (prato | drink | sobremesa | combo), is_active
2. Tabela `benefit_rules` criada: id, restaurant_id, available_days (array), available_hours_start, available_hours_end, daily_limit, is_active
3. CRUD de itens do beneficio no painel do restaurante
4. Categorias selecionaveis: Prato, Drink, Sobremesa, Combo
5. Configuracao de regras: dias da semana disponiveis, horario de inicio/fim, limite diario de cupons
6. Toggle para ativar/desativar beneficio individualmente
7. Toggle geral para pausar todos os beneficios temporariamente
8. RLS garante que restaurante so ve/edita seus proprios beneficios

## Tasks / Subtasks
- [ ] Verificar/criar tabelas `benefits` e `benefit_rules` na migration (AC: 1, 2)
  - [ ] Confirmar que DDL do Epic 1 ja criou as tabelas
  - [ ] Se nao, criar migration: `supabase/migrations/YYYYMMDD_create_benefits.sql`
  - [ ] Verificar ENUM `benefit_category` existe: `('prato', 'drink', 'sobremesa', 'combo')`
- [ ] Criar pagina de beneficios `apps/restaurant-web/app/(dashboard)/benefits/page.tsx` (AC: 3)
  - [ ] Listar beneficios: `supabase.from('benefits').select('*').eq('restaurant_id', myRestaurantId)`
  - [ ] Exibir em cards ou tabela: nome, categoria (badge), status (ativo/inativo)
  - [ ] Botao "Adicionar Beneficio" abre modal/dialog
- [ ] Implementar formulario de criacao/edicao de beneficio (AC: 3, 4)
  - [ ] Campos: name, description, category (select com 4 opcoes)
  - [ ] React Hook Form + Zod validation
  - [ ] Insert: `supabase.from('benefits').insert({ restaurant_id, name, description, category })`
  - [ ] Update: `supabase.from('benefits').update({...}).eq('id', benefitId)`
  - [ ] Delete: `supabase.from('benefits').delete().eq('id', benefitId)` com confirmacao
- [ ] Implementar select de categorias (AC: 4)
  - [ ] Dropdown com opcoes: Prato, Drink, Sobremesa, Combo
  - [ ] Badge colorido por categoria na listagem
- [ ] Implementar configuracao de regras de disponibilidade (AC: 5)
  - [ ] Secao separada "Regras de Disponibilidade" na pagina de beneficios
  - [ ] Checkboxes para dias da semana (Dom, Seg, Ter, Qua, Qui, Sex, Sab)
  - [ ] Time pickers para horario de inicio e fim
  - [ ] Input numerico para limite diario de cupons (default: 50)
  - [ ] Carregar: `supabase.from('benefit_rules').select('*').eq('restaurant_id', myRestaurantId)`
  - [ ] Salvar: upsert na tabela `benefit_rules`
- [ ] Implementar toggle individual de ativar/desativar beneficio (AC: 6)
  - [ ] Switch/toggle na row de cada beneficio
  - [ ] `supabase.from('benefits').update({ is_active: !current }).eq('id', benefitId)`
  - [ ] Feedback visual imediato (otimistic update)
- [ ] Implementar toggle geral para pausar todos os beneficios (AC: 7)
  - [ ] Switch no topo da pagina: "Pausar todos os beneficios"
  - [ ] Quando ativado: `supabase.from('benefit_rules').update({ is_active: false }).eq('restaurant_id', myRestaurantId)`
  - [ ] Quando desativado: `supabase.from('benefit_rules').update({ is_active: true }).eq('restaurant_id', myRestaurantId)`
  - [ ] Banner de aviso quando pausado: "Seus beneficios estao temporariamente pausados"
- [ ] Verificar RLS policies (AC: 8)
  - [ ] Testar que restaurant_admin so ve beneficios do proprio restaurante
  - [ ] Testar que restaurant_admin NAO consegue ver/editar beneficios de outro restaurante

## Dev Notes

**Tabela `benefits` (DDL do Architecture doc):**
```sql
CREATE TABLE benefits (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  restaurant_id UUID NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  name TEXT NOT NULL,
  description TEXT,
  category benefit_category NOT NULL,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT now()
);

-- Index para busca por restaurante
CREATE INDEX idx_benefits_restaurant ON benefits(restaurant_id) WHERE is_active = true;
```

**Tabela `benefit_rules` (DDL do Architecture doc):**
```sql
CREATE TABLE benefit_rules (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  restaurant_id UUID NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  available_days INT[] DEFAULT '{0,1,2,3,4,5,6}',  -- 0=dom, 1=seg, ..., 6=sab
  available_hours_start TIME DEFAULT '00:00',
  available_hours_end TIME DEFAULT '23:59',
  daily_limit INT DEFAULT 50,
  is_active BOOLEAN DEFAULT true,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

**ENUM `benefit_category`:**
```sql
CREATE TYPE benefit_category AS ENUM ('prato', 'drink', 'sobremesa', 'combo');
```

**RLS Policies (restaurante so ve/edita seus proprios beneficios):**
```sql
-- Benefits
CREATE POLICY "Anyone can view active benefits" ON benefits FOR SELECT USING (is_active = true);
CREATE POLICY "Restaurant admin manages own benefits" ON benefits FOR ALL USING (restaurant_id = get_my_restaurant_id());
CREATE POLICY "Super admin full access" ON benefits FOR ALL USING (get_user_role() = 'super_admin');

-- Benefit Rules
CREATE POLICY "Anyone can view active rules" ON benefit_rules FOR SELECT USING (is_active = true);
CREATE POLICY "Restaurant admin manages own rules" ON benefit_rules FOR ALL USING (restaurant_id = get_my_restaurant_id());
CREATE POLICY "Super admin full access" ON benefit_rules FOR ALL USING (get_user_role() = 'super_admin');

-- Helper function usada nas policies acima
CREATE OR REPLACE FUNCTION get_my_restaurant_id()
RETURNS UUID AS $$
  SELECT id FROM restaurants WHERE admin_user_id = auth.uid() LIMIT 1;
$$ LANGUAGE sql SECURITY DEFINER STABLE;
```

**TypeScript Interfaces (packages/shared):**
```typescript
export type BenefitCategory = 'prato' | 'drink' | 'sobremesa' | 'combo';

export interface Benefit {
  id: string;
  restaurant_id: string;
  name: string;
  description: string;
  category: BenefitCategory;
  is_active: boolean;
}

export interface BenefitRule {
  id: string;
  restaurant_id: string;
  available_days: number[]; // 0=dom, 1=seg, ..., 6=sab
  available_hours_start: string; // HH:mm
  available_hours_end: string;
  daily_limit: number;
  is_active: boolean;
}
```

**Zod Schemas para validacao:**
```typescript
import { z } from 'zod'

export const benefitSchema = z.object({
  name: z.string().min(2, 'Nome do beneficio obrigatorio'),
  description: z.string().optional(),
  category: z.enum(['prato', 'drink', 'sobremesa', 'combo'], {
    errorMap: () => ({ message: 'Selecione uma categoria' }),
  }),
})

export const benefitRuleSchema = z.object({
  available_days: z.array(z.number().min(0).max(6)).min(1, 'Selecione pelo menos 1 dia'),
  available_hours_start: z.string().regex(/^\d{2}:\d{2}$/, 'Formato HH:mm'),
  available_hours_end: z.string().regex(/^\d{2}:\d{2}$/, 'Formato HH:mm'),
  daily_limit: z.number().min(1, 'Minimo 1 cupom/dia').max(999),
})
```

**Rotas do restaurant-web:**
```
apps/restaurant-web/app/(dashboard)/benefits/
├── page.tsx              # Listagem de beneficios + regras de disponibilidade
```

**Mapeamento de dias para UI:**
```typescript
const WEEKDAYS = [
  { value: 0, label: 'Dom' },
  { value: 1, label: 'Seg' },
  { value: 2, label: 'Ter' },
  { value: 3, label: 'Qua' },
  { value: 4, label: 'Qui' },
  { value: 5, label: 'Sex' },
  { value: 6, label: 'Sab' },
]
```

### Testing
- Unit: Zod schema validation (benefitSchema, benefitRuleSchema)
- Integration: CRUD completo de benefits via Supabase
- Integration: RLS — restaurant_admin so ve/edita proprios beneficios
- Integration: RLS — usuario anonimo consegue ler beneficios ativos (para o app cliente)
- UI: Toggle individual atualiza status corretamente
- UI: Toggle geral pausa/retoma todos os beneficios
- Manual: Testar fluxo completo no browser (criar, editar, deletar, configurar regras)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 + Architecture v1.0 | Claude (@sm) |
