# Story 1.3: Auth System with 3 Roles

## Status
Approved

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["turbo typecheck", "turbo test"]

## Story
**As a** user/restaurant/admin,
**I want** to authenticate with role-appropriate access,
**so that** each user type sees only what is relevant to them.

## Acceptance Criteria
1. Supabase Auth configurado com email/senha para todos os roles
2. Login social (Google, Apple) configurado para role `user` no Supabase Dashboard/config
3. Trigger `on_auth_user_created` cria registro em `profiles` com role default `user` (implementado na Story 1.2, verificar funcionamento)
4. Endpoint/funcao para promover usuario a `restaurant_admin` (usado pelo admin +um via RPC ou Edge Function)
5. RLS policies diferenciam acesso por role: user ve apenas seus dados, restaurant_admin ve dados do seu restaurante, super_admin ve tudo (implementado na Story 1.2, verificar com testes)
6. `packages/shared` exporta funcoes utilitarias de auth: `getCurrentUser()`, `getUserRole()`, `isAuthenticated()`
7. Recuperacao de senha funcional via Supabase Auth (configurar template de email)
8. Tokens com refresh rotation configurado no Supabase Auth config

## Tasks / Subtasks
- [ ] Configurar Supabase Auth providers (AC: 1, 2)
  - [ ] Habilitar email/senha no `config.toml` (ja habilitado por padrao)
  - [ ] Configurar Google OAuth provider (client_id, secret no .env)
  - [ ] Configurar Apple OAuth provider (client_id, secret, team_id no .env)
  - [ ] Documentar variaveis de ambiente necessarias no `.env.example`
- [ ] Verificar trigger `handle_new_user` (AC: 3)
  - [ ] Testar criacao de usuario via `supabase.auth.signUp()`
  - [ ] Verificar que profile e criado com role='user' e full_name do metadata
  - [ ] Testar com e sem `full_name` no metadata
- [ ] Criar funcao de promocao de role (AC: 4)
  - [ ] RPC `promote_to_restaurant_admin(p_user_id UUID, p_restaurant_id UUID)` — SECURITY DEFINER, so super_admin pode chamar
  - [ ] Funcao atualiza `profiles.role` para 'restaurant_admin' e `restaurants.admin_user_id` para o user_id
  - [ ] Adicionar RLS check: apenas super_admin pode executar
- [ ] Verificar RLS policies por role (AC: 5)
  - [ ] Testar como `user`: so ve proprio profile, coupons, conversations
  - [ ] Testar como `restaurant_admin`: ve dados do proprio restaurante
  - [ ] Testar como `super_admin`: ve tudo
- [ ] Criar auth utils em `packages/shared` (AC: 6)
  - [ ] `packages/shared/src/auth/index.ts`
  - [ ] `getCurrentUser(supabase)` — retorna Profile do usuario logado
  - [ ] `getUserRole(supabase)` — retorna UserRole do usuario logado
  - [ ] `isAuthenticated(supabase)` — retorna boolean
  - [ ] `signIn(supabase, email, password)` — wrapper do auth.signInWithPassword
  - [ ] `signUp(supabase, email, password, fullName)` — wrapper com metadata
  - [ ] `signOut(supabase)` — wrapper do auth.signOut
  - [ ] `resetPassword(supabase, email)` — wrapper do auth.resetPasswordForEmail
  - [ ] Exportar de `packages/shared/src/index.ts`
- [ ] Configurar recuperacao de senha (AC: 7)
  - [ ] Configurar redirect URL no config.toml
  - [ ] Customizar template de email (PT-BR) se possivel no config local
- [ ] Configurar refresh token rotation (AC: 8)
  - [ ] Verificar `config.toml` tem `refresh_token_rotation_enabled = true`
  - [ ] Configurar `access_token_expiry = 3600` (1 hora)
  - [ ] Configurar `refresh_token_expiry = 604800` (7 dias)
- [ ] Escrever testes de integracao para auth flows
  - [ ] Teste: signup cria profile com role 'user'
  - [ ] Teste: signIn retorna session valida
  - [ ] Teste: getUserRole retorna role correto
  - [ ] Teste: promote_to_restaurant_admin funciona como super_admin
  - [ ] Teste: promote_to_restaurant_admin falha como user (RLS)

## Dev Notes

### Supabase Auth Config (config.toml)

```toml
[auth]
enabled = true
site_url = "http://localhost:3000"
additional_redirect_urls = ["http://localhost:3000", "http://localhost:3001", "exp://localhost:8081"]

[auth.email]
enable_signup = true
double_confirm_changes = true
enable_confirmations = false  # Desabilitar em dev, habilitar em prod

[auth.external.google]
enabled = true
client_id = "env(GOOGLE_CLIENT_ID)"
secret = "env(GOOGLE_CLIENT_SECRET)"
redirect_uri = "http://localhost:54321/auth/v1/callback"

[auth.external.apple]
enabled = true
client_id = "env(APPLE_CLIENT_ID)"
secret = "env(APPLE_CLIENT_SECRET)"
redirect_uri = "http://localhost:54321/auth/v1/callback"
```

### Trigger handle_new_user (ja implementado na Story 1.2)

```sql
CREATE OR REPLACE FUNCTION handle_new_user()
RETURNS TRIGGER AS $$
BEGIN
  INSERT INTO profiles (id, full_name, role)
  VALUES (
    NEW.id,
    COALESCE(NEW.raw_user_meta_data->>'full_name', ''),
    'user'
  );
  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION handle_new_user();
```

### RPC: Promover usuario a restaurant_admin

```sql
CREATE OR REPLACE FUNCTION promote_to_restaurant_admin(p_user_id UUID, p_restaurant_id UUID)
RETURNS BOOLEAN AS $$
BEGIN
  -- Apenas super_admin pode chamar
  IF get_user_role() != 'super_admin' THEN
    RAISE EXCEPTION 'Apenas super_admin pode promover usuarios';
  END IF;

  -- Atualizar role do profile
  UPDATE profiles SET role = 'restaurant_admin' WHERE id = p_user_id;

  -- Vincular ao restaurante
  UPDATE restaurants SET admin_user_id = p_user_id WHERE id = p_restaurant_id;

  RETURN true;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;
```

### Auth Utils (packages/shared)

```typescript
// packages/shared/src/auth/index.ts
import { SupabaseClient } from '@supabase/supabase-js'
import { Profile, UserRole } from '../types/database'

export async function getCurrentUser(supabase: SupabaseClient): Promise<Profile | null> {
  const { data: { user } } = await supabase.auth.getUser()
  if (!user) return null

  const { data: profile } = await supabase
    .from('profiles')
    .select('*')
    .eq('id', user.id)
    .single()

  return profile
}

export async function getUserRole(supabase: SupabaseClient): Promise<UserRole | null> {
  const profile = await getCurrentUser(supabase)
  return profile?.role ?? null
}

export async function isAuthenticated(supabase: SupabaseClient): Promise<boolean> {
  const { data: { user } } = await supabase.auth.getUser()
  return !!user
}

export async function signIn(supabase: SupabaseClient, email: string, password: string) {
  return supabase.auth.signInWithPassword({ email, password })
}

export async function signUp(supabase: SupabaseClient, email: string, password: string, fullName: string) {
  return supabase.auth.signUp({
    email,
    password,
    options: { data: { full_name: fullName } },
  })
}

export async function signOut(supabase: SupabaseClient) {
  return supabase.auth.signOut()
}

export async function resetPassword(supabase: SupabaseClient, email: string) {
  return supabase.auth.resetPasswordForEmail(email, {
    redirectTo: `${window.location.origin}/reset-password`,
  })
}
```

### Supabase Client Setup (referencia da arquitetura)

**Mobile (apps/mobile/services/supabase.ts):**
```typescript
import { createClient } from '@supabase/supabase-js'
import AsyncStorage from '@react-native-async-storage/async-storage'
import { Database } from '@maisum/shared/types/supabase'

export const supabase = createClient<Database>(
  process.env.EXPO_PUBLIC_SUPABASE_URL!,
  process.env.EXPO_PUBLIC_SUPABASE_ANON_KEY!,
  {
    auth: {
      storage: AsyncStorage,
      autoRefreshToken: true,
      persistSession: true,
      detectSessionInUrl: false,
    },
  }
)
```

**Admin Web Browser (apps/admin-web/lib/supabase/client.ts):**
```typescript
import { createBrowserClient } from '@supabase/ssr'
import { Database } from '@maisum/shared/types/supabase'

export const createClient = () =>
  createBrowserClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!
  )
```

**Admin Web Server (apps/admin-web/lib/supabase/server.ts):**
```typescript
import { createServerClient } from '@supabase/ssr'
import { cookies } from 'next/headers'
import { Database } from '@maisum/shared/types/supabase'

export const createClient = () => {
  const cookieStore = cookies()
  return createServerClient<Database>(
    process.env.NEXT_PUBLIC_SUPABASE_URL!,
    process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY!,
    { cookies: { getAll: () => cookieStore.getAll() } }
  )
}
```

### Dependencias desta story
- **Depende de:** Story 1.2 (schema, RLS, trigger ja existem)
- **Bloqueia:** Stories 1.4, 1.5, 1.6 (precisam do auth system)

### Variaveis de ambiente necessarias

```bash
# .env.example (adicionar)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
APPLE_CLIENT_ID=
APPLE_CLIENT_SECRET=
```

## Testing
- Testar signup com email/senha: verificar profile criado com role='user'
- Testar signup com full_name no metadata: verificar que full_name e salvo
- Testar signIn: verificar session retornada com access_token e refresh_token
- Testar getUserRole: verificar retorno correto para cada role
- Testar isAuthenticated: true quando logado, false quando deslogado
- Testar promote_to_restaurant_admin como super_admin: sucesso
- Testar promote_to_restaurant_admin como user: falha (permissao negada)
- Testar resetPassword: verificar que email e enviado (verificar logs do Supabase local)
- Testar refresh token: verificar que token e rotacionado apos uso
- Testar RLS: usuario user nao pode ver profiles de outros usuarios
- Testar RLS: restaurant_admin ve apenas dados do seu restaurante

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 e Architecture v1.0 | River (@sm) |
