# Story 2.2: Restaurant Invite Link & Self-Registration

## Status
Draft

## Executor Assignment
executor: "@dev"
quality_gate: "@architect"
quality_gate_tools: ["turbo build", "turbo lint", "turbo typecheck"]

## Story
**As a** super admin,
**I want** to generate invite links for restaurants,
**so that** restaurant owners can self-register on the platform.

## Acceptance Criteria
1. Botao "Gerar Link de Cadastro" na pagina de restaurantes do admin
2. Link gerado contem token unico com expiracao de 7 dias
3. Tabela `restaurant_invites` armazena: token, restaurant_id (pre-criado), expires_at, used_at
4. Pagina publica de cadastro do restaurante (acessivel via link)
5. Restaurante preenche: nome do responsavel, email, senha
6. Ao submeter, cria usuario com role `restaurant_admin` e vincula ao restaurante pre-criado
7. Token marcado como usado apos registro
8. Link expirado ou ja usado mostra mensagem apropriada

## Tasks / Subtasks
- [ ] Implementar botao "Gerar Link de Cadastro" na listagem de restaurantes (AC: 1)
  - [ ] Botao na row da tabela ou em dialog de detalhes
  - [ ] Chamar `supabase.from('restaurant_invites').insert({ restaurant_id })` ao clicar
  - [ ] Exibir modal com link copiavel (`{BASE_URL}/invite/{token}`)
  - [ ] Botao de copiar link para clipboard
- [ ] Garantir que o token gerado tem expiracao de 7 dias (AC: 2)
  - [ ] Verificar que DDL ja define `DEFAULT (now() + INTERVAL '7 days')` para `expires_at`
  - [ ] Exibir data de expiracao no modal do link gerado
- [ ] Verificar/criar tabela `restaurant_invites` na migration (AC: 3)
  - [ ] Confirmar que migration do Epic 1 ja criou a tabela
  - [ ] Se nao, criar migration: `supabase/migrations/YYYYMMDD_create_restaurant_invites.sql`
- [ ] Criar pagina publica de registro `apps/admin-web/app/invite/[token]/page.tsx` (AC: 4)
  - [ ] Pagina NAO protegida por auth middleware (publica)
  - [ ] Ao carregar, validar token: `supabase.from('restaurant_invites').select('*, restaurants(name)').eq('token', token).single()`
  - [ ] Verificar: `used_at IS NULL AND expires_at > now()`
  - [ ] Exibir nome do restaurante associado ao convite
- [ ] Implementar formulario de registro do responsavel (AC: 5)
  - [ ] Campos: nome completo, email, senha, confirmar senha
  - [ ] Validacao Zod: email valido, senha min 8 chars, senhas coincidem
- [ ] Implementar fluxo de criacao do usuario restaurant_admin (AC: 6)
  - [ ] Chamar `supabase.auth.signUp({ email, password, options: { data: { full_name } } })`
  - [ ] Atualizar profile: `supabase.from('profiles').update({ role: 'restaurant_admin' }).eq('id', user.id)`
  - [ ] Vincular ao restaurante: `supabase.from('restaurants').update({ admin_user_id: user.id }).eq('id', invite.restaurant_id)`
  - [ ] Usar Edge Function ou service_role para operacoes privilegiadas
- [ ] Marcar token como usado apos registro (AC: 7)
  - [ ] `supabase.from('restaurant_invites').update({ used_at: new Date().toISOString() }).eq('token', token)`
  - [ ] Executar atomicamente com criacao do usuario (ou rollback)
- [ ] Implementar tratamento de link expirado/usado (AC: 8)
  - [ ] Se `used_at IS NOT NULL`: exibir "Este link ja foi utilizado"
  - [ ] Se `expires_at < now()`: exibir "Este link expirou"
  - [ ] Se token nao encontrado: exibir "Link invalido"
  - [ ] Em todos os casos, oferecer link para contato do admin +um

## Dev Notes

**Tabela `restaurant_invites` (DDL do Architecture doc):**
```sql
CREATE TABLE restaurant_invites (
  id UUID PRIMARY KEY DEFAULT uuid_generate_v4(),
  restaurant_id UUID NOT NULL REFERENCES restaurants(id) ON DELETE CASCADE,
  token TEXT UNIQUE NOT NULL DEFAULT encode(gen_random_bytes(32), 'hex'),
  expires_at TIMESTAMPTZ NOT NULL DEFAULT (now() + INTERVAL '7 days'),
  used_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

**RLS Policies para invites:**
```sql
-- Super admin gerencia convites
CREATE POLICY "Super admin manages invites" ON restaurant_invites FOR ALL USING (get_user_role() = 'super_admin');

-- Publico pode ler convites validos (para a pagina de registro)
CREATE POLICY "Public can read valid invites" ON restaurant_invites FOR SELECT USING (used_at IS NULL AND expires_at > now());
```

**TypeScript Interface (packages/shared):**
```typescript
export interface RestaurantInvite {
  id: string;
  restaurant_id: string;
  token: string;
  expires_at: string;
  used_at: string | null;
  created_at: string;
}
```

**Fluxo de auth para criar restaurant_admin:**

O fluxo requer operacoes privilegiadas (mudar role, vincular restaurante). Opcoes:
1. **Edge Function (recomendado):** Criar `supabase/functions/register-restaurant-admin/index.ts` que recebe `{ token, email, password, full_name }`, valida o invite, cria usuario via `supabase.auth.admin.createUser()`, atualiza profile e restaurante, marca invite como usado — tudo atomicamente com service_role.
2. **Client-side + trigger:** Menos seguro, depende de trigger para promover role.

```typescript
// supabase/functions/register-restaurant-admin/index.ts
import { serve } from 'https://deno.land/std/http/server.ts'
import { createClient } from 'https://esm.sh/@supabase/supabase-js'

serve(async (req: Request) => {
  const { token, email, password, full_name } = await req.json()
  const supabase = createClient(
    Deno.env.get('SUPABASE_URL')!,
    Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!
  )

  // 1. Validar invite
  const { data: invite } = await supabase
    .from('restaurant_invites')
    .select('*')
    .eq('token', token)
    .is('used_at', null)
    .gt('expires_at', new Date().toISOString())
    .single()

  if (!invite) return new Response(JSON.stringify({ error: 'Convite invalido ou expirado' }), { status: 400 })

  // 2. Criar usuario
  const { data: { user }, error } = await supabase.auth.admin.createUser({
    email, password, email_confirm: true,
    user_metadata: { full_name }
  })
  if (error) return new Response(JSON.stringify({ error: error.message }), { status: 400 })

  // 3. Atualizar profile para restaurant_admin
  await supabase.from('profiles').update({ role: 'restaurant_admin', full_name }).eq('id', user!.id)

  // 4. Vincular ao restaurante
  await supabase.from('restaurants').update({ admin_user_id: user!.id }).eq('id', invite.restaurant_id)

  // 5. Marcar invite como usado
  await supabase.from('restaurant_invites').update({ used_at: new Date().toISOString() }).eq('id', invite.id)

  return new Response(JSON.stringify({ success: true }), { status: 200 })
})
```

**Rota publica (excluir do auth middleware):**
```typescript
// apps/admin-web/middleware.ts — adicionar excecao
export const config = {
  matcher: ['/((?!_next/static|_next/image|favicon.ico|login|invite).*)']
}
```

**Estrutura de rotas:**
```
apps/admin-web/app/
├── invite/
│   └── [token]/page.tsx    # Pagina publica de registro (SEM auth)
├── (dashboard)/restaurants/
│   └── page.tsx             # Botao "Gerar Link" aqui
```

### Testing
- Unit: Validacao Zod do form de registro (email, senha, nome)
- Integration: Teste de fluxo completo — criar invite, acessar pagina, registrar, verificar usuario criado
- Integration: Teste de invite expirado/usado retorna erro
- RLS: Verificar que usuario anonimo consegue ler invite valido mas nao outros
- Manual: Testar fluxo completo no browser (gerar link, abrir, registrar, fazer login)

## Change Log
| Date | Version | Description | Author |
|------|---------|-------------|--------|
| 2026-03-19 | 1.0 | Story criada a partir do PRD v1.0 + Architecture v1.0 | Claude (@sm) |
